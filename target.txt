# Homelab VM scan targets
# Add one expected VM IP, hostname, or CIDR per line.
# Blank lines and lines beginning with # are ignored.
#
# Inline comments after an IP are NOT supported — put labels on the
# preceding comment line, as shown below.
#
# If you add a CIDR here, the host is treated as an expected scan range.
# For unrecognized-host detection, set DISCOVERY_CIDRS in the workflow.

# Proxmox VM inventory
# ----------------------------------------------------------------
# Kali at 192.168.3.149 is the attacker/scanner host (self-hosted
# GitHub Actions runner). It is intentionally NOT listed as a scan
# target.
#
# Ubuntu Server
192.168.3.60

# Windows 11 (current primary target)
192.168.3.174
